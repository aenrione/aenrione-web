version: "3.9"

services:
  aenrione-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aenrione-web
    restart: unless-stopped
    ports:
      - "4321:4321"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=4321
